## 1. NodeJS
### 1.1.
```javascript
const handle = (a) => {
  let dict = {};
  let words = a.split(" ");
  words.forEach(w => {
    let t = w.toLowerCase().replace(".", "");
    dict[t] = (dict[t] ?? 0) + 1;
  })
  return dict;
}
```
### 1.2.

Use ExpressJS as a web framework for handling HTTP requests
- Authentication
> Passport 
